{
  "name": "hub-registry-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-hub @tindevelopers/* registry-truth report (T12, Shared Hubs Baseline). See REGISTER.md.",
  "packageManager": "pnpm@10.6.1",
  "engines": {
    "node": "24.x"
  },
  "type": "module",
  "scripts": {
    "register": "node scripts/generate-register.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.5"
  }
}
